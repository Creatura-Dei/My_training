1) git diff - проверка, что поменялось

Команда сравнивает последнюю закомиченную версию файла с текущей (изменённой) версией. 

По умолчанию команда git diff не показывает изменения в staged-файлах - modified. 

Чтобы просмотреть изменения - используем флаг --staged
    
    git diff --staged 